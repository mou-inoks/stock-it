const ComputerStatusEnum = Object.freeze({
    AVAILABLE: 1,
    BORROWED: 2,
});

const LoanTypeEnum = Object.freeze({
    COMPUTER: 1,
    DEVICE: 2,
});

export default { ComputerStatusEnum, LoanTypeEnum };